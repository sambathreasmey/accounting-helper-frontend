# Base URL of your FastAPI backend (no trailing slash), e.g.:
# NEXT_PUBLIC_API_BASE_URL=https://your-app.fastapicloud.dev
#
# Leave empty if the Mini App and API share the same origin
# (e.g. you put a reverse proxy in front of both).
NEXT_PUBLIC_API_BASE_URL=

